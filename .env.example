# One or more Groq API keys, comma-separated.
# The server rotates to the next key on a 429 and retries the same request.
GROQ_API_KEYS=gsk_your_first_key,gsk_your_second_key

# Single-key fallback (used only if GROQ_API_KEYS is unset).
# GROQ_API_KEY=gsk_your_key
